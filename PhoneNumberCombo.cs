public class Solution
{
    public IList<string> LetterCombinations(string digits)
    {
        char[] two = ['a', 'b', 'c'];
        char[] three = ['d', 'e', 'f'];
        char[] four = ['g', 'h', 'i'];
        char[] five = ['j', 'k', 'l'];
        char[] six = ['m', 'n', 'o'];
        char[] seven = ['p', 'q', 'r', 's'];
        char[] eight = ['t', 'u', 'v'];
        char[] nine = ['w', 'x', 'y', 'z'];

        List<string> combos = new List<string>();

        var word = '';
        for (int i = 0; i < digits.Length; i++)
        {
            var array = two;
            for (int j = 0; j < array.Length; j++)
            {
                word += c;
            }
        }
    }

    public IList<string> LetterCombinations(string digits, List)
    {
    }